import type {Gap} from "@/lib/types";
export default function SkillsChart({gaps}:{gaps:Gap[]}){return <div className="skill-list">{gaps.map(g=><div className="skill-row" key={g.skill_id}><div className="skill-name"><span>{g.name}</span>{g.critical&&<span className="critical">Ключевой</span>}</div><div className="skill-track"><div className="skill-fill" style={{width:`${Math.min(100,g.current/5*100)}%`}}/><div className="skill-marker" style={{left:`${Math.min(100,g.required/5*100)}%`}}/></div><div className="skill-level">{g.current}<span> / {g.required}</span></div></div>)}</div>}
