# false: real path through the Go API (docker compose default). true: bundled kit in the browser.
NEXT_PUBLIC_USE_MOCKS=false
NEXT_PUBLIC_API_URL=http://localhost:8080
# Only used with mocks: call the AI service directly (demo stopgap). Leave empty to stay fully local.
NEXT_PUBLIC_AI_URL=http://localhost:8001
