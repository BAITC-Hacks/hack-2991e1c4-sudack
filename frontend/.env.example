# Local kit in the browser (true) or the Go API (false)
NEXT_PUBLIC_USE_MOCKS=true
NEXT_PUBLIC_API_URL=http://localhost:8080
# With mocks on, set this to call the AI service directly for live LLM explanations, roadmap, risk and impact.
# Leave empty for fully offline template explanations.
NEXT_PUBLIC_AI_URL=http://localhost:8001
