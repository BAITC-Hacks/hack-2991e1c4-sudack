# Copy to .env before `docker compose up`. All values are optional.
OPENAI_API_KEY=
NVIDIA_API_KEY=
LLM_MODEL=gpt-4o
LLM_TIMEOUT=8
# Frontend: "true" runs on the bundled synthetic dataset, "false" talks to the Go API.
FRONTEND_USE_MOCKS=true
FRONTEND_API_URL=http://localhost:8080
